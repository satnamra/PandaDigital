I can’t write to the filesystem in this environment (read-only sandbox), so I’m providing the exact `manifest.xml` content for you to drop into `PandaDigital/manifest.xml`.

```xml
<!-- App Manifest -->
<iq:manifest xmlns:iq="http://www.garmin.com/xml/connectiq" version="3">
    <iq:application
        entry="PandaDigitalApp"
        id="22cb517e-e3cf-4666-b6bd-f000af785239"
        launcherIcon="@Drawables.LauncherIcon"
        minApiLevel="4.0.0"
        name="@Strings.AppName"
        type="watchface">

        <iq:products>
            <iq:product id="fenix7"/>
            <iq:product id="venu3"/>
        </iq:products>

        <iq:permissions>
            <iq:uses-permission id="SensorHistory"/>
            <iq:uses-permission id="UserProfile"/>
        </iq:permissions>

        <iq:languages>
            <iq:language>eng</iq:language>
        </iq:languages>

        <iq:barrels/>
    </iq:application>
</iq:manifest>
```

If you want, tell me your preferred `minApiLevel` and I’ll adjust it.
